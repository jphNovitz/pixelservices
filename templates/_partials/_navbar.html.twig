<div class="w-full bg-gradient from-bg-base-100 to-base-40 backdrop-blur-lg fixed top-0 left-0 z-50 ">
    <div class="navbar max-w-7xl m-auto">
        <div class="navbar-start">
            <div class="dropdown">
                <div tabindex="0" role="button" class="btn btn-ghost lg:hidden">
                    <svg
                            xmlns="http://www.w3.org/2000/svg"
                            class="h-5 w-5"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor">
                        <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M4 6h16M4 12h8m-8 6h16"/>
                    </svg>
                </div>
                <ul
                        tabindex="0"
                        class="menu menu-sm dropdown-content rounded-box z-[1] w-screen h-96 mt-3 w-52 p-2 shadow bg-base-300">
                    <li class="">
                        <a class="{% if (app.request.get('_route') == 'app_home') %} link link-primary {% endif %}"
                           href="{{ path('app_home') }}">Accueil</a>
                    </li>
                    <li class="{% if (app.request.get('_route') == 'app_works_list') %} link link-primary {% endif %}">
                        <a class="" href="{{ path('app_works_list') }}">Offre</a></li>
                    <li class="{% if (app.request.get('_route') == 'app_contact') %} link link-primary {% endif %}">
                        <a class="" href="{{ path('app_contact') }}">Contact</a></li>
                </ul>
            </div>
            <twig:Logo class="w-16"/>
            
        </div>
        <div class="navbar-center hidden lg:flex">
            <ul class="menu menu-horizontal mb-0">
                <li class="p-2">
                    <a class="{% if (app.request.get('_route') == 'app_home') %} link link-primary {% endif %}"
                       href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="{% if (app.request.get('_route') == 'app_works_list') %} link link-primary {% endif %}"><a
                            class="" href="{{ path('app_works_list') }}">Offre</a></li>
                <li class="{% if (app.request.get('_route') == 'app_contact') %} link link-primary {% endif %}"><a
                            class="" href="{{ path('app_contact') }}">Contact</a></li>
            </ul>
        </div>
        <div class="navbar-end">
            {# <a class="btn" href="{{ path('app_contact') }}">Contact</a> #}
        </div>
    </div>
</div>
