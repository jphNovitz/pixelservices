{% extends 'base.html.twig' %}

{% block title %}Créons ensemble votre site internet | pixelservices{% endblock %}

{% block body %}
    {#         bg image, move to base if necesary #}
{#    {% if app.request.get('_route') == 'app_home' %}#}
        <div class="bg-hero-mobile md:bg-hero-md bg-cover bg-center absolute top-0 left-0 right-0 w-full h-2/3">
        </div>
{#    {% endif %}#}

    {% include '_partials/_home_cta.html.twig' %}

    <section class="py-16 my-16 bg-gray-50">
    <div class="container mx-auto px-4">
        <h2 class="text-2xl font-medium text-center text-gray-700 mb-12">
            Ils nous font confiance
        </h2>
        
        <div class="flex flex-wrap justify-center items-center gap-12">
            <a href="https://gc-bernissart.be/" target="_blank">
                <img src="{{asset('images/logos/logo-gais-carabiniers-bernissart.webp')}}" alt="Les Gais Carabiniers de Bernissart" 
                class="h-36 object-contain opacity-50 grayscale hover:opacity-100 hover:grayscale-0 transition-all duration-300 cursor-pointer">
            </a>
        </div>
    </div>
</section>

    <div class="w-full md:max-w-3xl lg:max-w-4xl m-auto">
        {% for article in pinned %}
        <twig:Blog:Article title="{{ article.title }}" content="{{ article.content }}" home="true"/>
        
        {% endfor %}
    </div>

{#    </div>#}

{% endblock %}
