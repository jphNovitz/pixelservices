{% extends 'base.html.twig' %}

{% block title %}Créons ensemble votre site internet | pixelservices{% endblock %}

{% block body %}
    {#         bg image, move to base if necesary #}
{#    {% if app.request.get('_route') == 'app_home' %}#}
        <div class="bg-hero-mobile md:bg-hero-md bg-cover bg-center absolute top-0 left-0 right-0 w-full h-2/3">
        </div>
{#    {% endif %}#}

    {% include '_partials/_home_cta.html.twig' %}

    <div class="w-full md:max-w-3xl lg:max-w-4xl m-auto">
        {% for article in pinned %}
        <twig:Blog:Article title="{{ article.title }}" content="{{ article.content }}" home="true"/>
            {# {{ component('Article', {
                'title' : article.title,
                'content' : article.content,
                'home': true
            }) }} #}
        {% endfor %}
    </div>

{#    </div>#}

{% endblock %}
