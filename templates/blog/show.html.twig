{% extends 'base.html.twig' %}

{% block title %}{{ post.title }} | pixelservices.be{% endblock %}
{% block og_title %}{{ post.title }} | pixelservices.be {% endblock %}
{% block og_image %}{{ asset("images/blog/"~image_url) }}{% endblock %}
{% block og_description %} {{ post.summary}} {% endblock %}
{% block metas %}
    <meta name="description"
          content=" {{ post.summary}}"/>
    <meta name="keywords" content="site web, ecommerce, vitrine, site vitrine, wp, wordpress, wix"/>
{% endblock %}

{% block body %}

    {{ component('Article', {
        'title' : post.title,
        'summary': post.summary,
        'content' : post.content,
        'image_url' : post.image,
        'image_alt' : post.title
    }) }}


    {{ component('Button', {
        href: path('app_blog_index'),
        label: 'Focus'
    }) }}


{#    <div class="w-1/2 after:content-[','] hover:bg-blue-800"> ddsq</div>#}
{% endblock %}
